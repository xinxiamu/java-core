package com.ymu.javase.thread;

//线程生命周期
//线程在其生命周期中经历不同的状态，包括新建（New）、就绪（Runnable）、运行（Running）、阻塞（Blocked）、等待（Waiting）、定时等待（Timed Waiting）和终止（Terminated）等状态。你可以使用线程的方法来管理和监控线程状态。

//线程调度
//Java 虚拟机负责线程的调度，决定哪个线程可以执行。线程调度器可能根据优先级、时间片轮转、公平性等策略来分配 CPU 时间给不同的线程。
